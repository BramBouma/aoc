digits = "[0-9]{1,3}"
within_mul = digits + "," + digits  # [0-9]{1,3},[0-9]{1,3}
mul = "mul\\(" + within_mul + "\\)"  # mul\([0-9]{1,3},[0-9]{1,3}\)

do = "do\\(\\)"  # do\(\)
dont = "don't\\(\\)"  # don't\(\)

# (?<=(do\(\))).*?(?=(don't\(\)))
# (do\(\))|(don't\(\))
# ((?<=(?:do\(\))).*?(?=(?:don't\(\))))


# (.*?)(?:don't\(\)(.*?))?do\(\)
